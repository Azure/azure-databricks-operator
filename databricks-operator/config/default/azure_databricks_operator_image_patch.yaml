apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: xinsnake/azure-databricks-operator:0.0.8
        name: manager
        env:
          - name: DATABRICKS_HOST
            valueFrom:
              secretKeyRef:
                name: dbrickssettings
                key: DatabricksHost
          - name: DATABRICKS_TOKEN
            valueFrom:
              secretKeyRef:
                name: dbrickssettings
                key: DatabricksToken